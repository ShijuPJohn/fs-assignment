import React from 'react';

function Footer(props) {
    return (
        <footer
            className={"flex items-center justify-between px-2 py-1 h-[10rem] w-full text-white bg-[var(--bg-header-footer)] text"}>
            This is a footer
        </footer>
    );
}

export default Footer;